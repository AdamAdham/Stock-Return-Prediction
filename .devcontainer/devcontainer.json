{
  "name": "TF GPU Project",
  "image": "tf-gpu-project1",
  "context": "..",
  "workspaceFolder": "/workspace",
  "runArgs": ["--gpus", "all"],
  "settings": {
    "terminal.integrated.defaultProfile.linux": "bash"
  },
  "mounts": ["source=${localWorkspaceFolder},target=/workspace,type=bind"]
}
